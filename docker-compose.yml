version: '3.7'

services:
  db:
    image: postgres:15-alpine
    container_name: siteplan_db
    command: >
      postgres
      -c log_statement=all
      -c max_connections=1000
      -c log_connections=true
      -c log_disconnections=true
      -c log_duration=true
    ports:
      - "5432:5432"
    environment:
      POSTGRES_PASSWORD: secret
    volumes:
      - ./.docker:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:5.0.5
    container_name: siteplan_redis
    ports:
      - "6379:6379"

  adminer:
    image: adminer
    container_name: siteplan_adminer
    command: ["php", "-S", "0.0.0.0:8081", "-t", "/var/www/html"]
    links:
      - "db:db"
    ports:
      - "8081:8081"
    environment:
      DATABASE_URL: "postgres://siteplan_dev:pgsql@db:5432/siteplan_dev?sslmode=disable"
    depends_on:
      db:
        condition: service_healthy
